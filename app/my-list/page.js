import MyListClient from './MyListClient';

export const metadata = {
  title: 'My List — WanderList',
  description: 'Your personal travel wishlist and visited destinations.',
};

export default function MyListPage() {
  return <MyListClient />;
}
